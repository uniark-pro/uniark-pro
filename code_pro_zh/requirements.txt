python-binance>=1.0.19
# 必须 <3.0：pandas 3.0 把 Copy-on-Write 设为强制默认，
# 派生数组变只读，会导致 "assignment destination is read-only"
pandas>=2.0.0,<3.0
numpy>=1.24.0
mplfinance>=0.12.10b0
matplotlib>=3.7.0
flask>=2.3.0
# 股票数据源：yfinance 为主，akshare 为备用。
# data_yfinance.py / data_akshare.py 用到，缺失会在画股票图时报
# "需要 xxx 包"。原 requirements 一直漏写了这两行。
yfinance>=1.3.0
akshare>=1.14.0