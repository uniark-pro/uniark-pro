python-binance>=1.0.19
# 必须 <3.0：pandas 3.0 把 Copy-on-Write 设为强制默认，
# 派生数组变只读，会导致 "assignment destination is read-only"
pandas>=2.0.0,<3.0
numpy>=1.24.0
mplfinance>=0.12.10b0
matplotlib>=3.7.0
flask>=2.3.0
# 股票数据源：yfinance 为主，akshare 为备用。
# data_yfinance.py / data_akshare.py 用到，缺失会在画股票图时报
# "需要 xxx 包"。原 requirements 一直漏写了这两行。
# yfinance 1.3.0+ 依赖 curl_cffi，要求 Python 3.10+；Python 3.9 用 0.2.x
# 系列即可，功能完整且更稳定（1.x 在 2025-2026 持续退化）。
yfinance>=0.2.40
akshare>=1.14.0
# Hyperliquid 数据源：HYPE / PURR 等 HL 原生币不在 binance 现货，
# data_hyperliquid.py 走 https://api.hyperliquid.xyz/info 直接拉 candleSnapshot。
# 只用到 POST/JSON，requests 已是 yfinance/akshare 的间接依赖，
# 写明确版本是为了让缺失时能直接定位（而非埋在 yfinance 安装失败的报错里）。
requests>=2.28.0
# 多用户版密码哈希：bcrypt 自带盐 + 自适应工作因子，比无盐 sha256 安全得多。
# 仅多用户版（code_pro_multiuser）需要；单用户版不依赖此包。
bcrypt>=4.0.0
